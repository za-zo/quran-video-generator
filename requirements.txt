# Quran Video Generator – Python dependencies
# Python 3.10+

pydantic>=2.5.0
pydantic-settings>=2.1.0
PyYAML>=6.0
python-dotenv>=1.0.0

# Cloud-native persistence (replaces SQLAlchemy/SQLite)
pymongo>=4.6.0

# Media download (streaming, with retries)
requests>=2.31.0

# Cloudinary upload (final MP4 delivery)
cloudinary>=1.38.0

# Audio analysis (silence detection for ayat-respecting cuts)
pydub>=0.25.1

# Testing
pytest>=7.4.0
mongomock>=4.1.0
